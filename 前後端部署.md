# 台科大Python課程 Week 8

# 互動式前後端網頁實作：GitHub Pages + Render

> 適合：第一次接觸前後端、API、部署的學生
> 
> 
> 技術：HTML + CSS + JavaScript + Python + FastAPI + GitHub Pages + Render
> 
> 教學方式：先下載完整專案，成功跑起來，再理解最重要的程式碼與部署流程。
> 

---

# 1. 今天要完成什麼？

今天會完成一個真正可以公開使用的簡易互動式網站。

使用者輸入文字後：

```
使用者
  ↓
Frontend
  ↓
JavaScript fetch()
  ↓
FastAPI Backend
  ↓
Python 處理
  ↓
JSON Response
  ↓
Frontend 更新畫面
```

最後還會把：

```
Frontend
```

部署到：

```
GitHub Pages
```

把：

```
Python Backend
```

部署到：

```
Render
```

完成後，其他人只要有網址，就可以使用你的網站。

---

# 2. 課程檔案

請先下載完整課程專案 ：

https://drive.google.com/drive/folders/1IwRCsy_KU1l6xna7jvED1T5s-QI_t8Nn?usp=sharing

下載後解壓縮。

專案結構：

```
simple-fullstack-render-demo/
│
├── backend/
│   ├── main.py
│   └── requirements.txt
│
└── frontend/
    ├── index.html
    ├── style.css
    └── script.js
```

跟Agent說 **幫我建一個.venv資料夾管理python版本，灌好python extension**

今天不需要從零輸入所有程式碼。

我們會：

```
先讓程式成功執行
↓
理解核心程式
↓
修改功能
↓
部署到 Internet

```

---

# 3. 今天只需要先懂五件事

先不用記很多語法。

記住：

- HTML：網頁有哪些東西
- CSS：網頁長什麼樣子
- JavaScript：網頁怎麼互動
- FastAPI：Python Backend
- API：Frontend 和 Backend 溝通的方法

一句話：

```
Frontend 負責跟使用者互動

Backend 負責處理資料

API 負責讓兩邊說話
```

---

# 4. 用 VS Code 打開專案

解壓縮後：

```
VS Code
↓
File
↓
Open Folder
```

選擇：

```
simple-fullstack-render-demo
```

左邊應該看到：

```
backend
frontend
README.md
```

---

# 5. 第一步：先把 Backend 跑起來

在 VS Code 開啟 Terminal。

進入：

```bash
cd backend
```

安裝需要的 Python 套件：

```bash
pip install -r requirements.txt
```

啟動 FastAPI：

```bash
uvicorn main:app --reload
```

成功後，Terminal 應該會出現類似：

```
Uvicorn running on http://127.0.0.1:8000
```

---

# 6. 測試 Backend

瀏覽器打開：

```
http://127.0.0.1:8000
```

正常的話會看到：

```json
{
  "message": "Backend is running!"
}
```

這代表：

```
Python Backend 已經成功執行
```

---

# 7. FastAPI 最方便的地方：/docs

打開：

```
http://127.0.0.1:8000/docs
```

這是 FastAPI 自動產生的 API 測試介面。

找到：

```
POST /api/message
```

按：

```
Try it out
```

輸入：

```json
{
  "message": "Hello"
}
```

按 Execute。

應該會收到類似：

```json
{
  "success": true,
  "reply": "Hello! 很高興認識你！"
}
```

如果這裡成功：

```
Backend API 正常
```

---

# 8. Backend 最重要的程式碼

現在打開：

```
backend/main.py
```

今天不用逐行看完整程式。

最重要的是這一段：

```python
@app.post("/api/message")
def receive_message(data: MessageRequest):

    return {
        "success": True,
        "reply": f"後端收到你的訊息：{data.message}"
    }
```

可以理解成：

```
Frontend 傳資料到：

POST /api/message

        ↓

Python 執行：

receive_message()

        ↓

取得：

data.message

        ↓

回傳 JSON
```

---

# 9. POST 是什麼？

今天先簡單理解：

```
GET
```

通常是：

```
我要取得資料
```

例如：

```
GET /
```

---

```
POST
```

通常是：

```
我要送資料給 Backend
```

例如：

```
POST /api/message
```

送：

```json
{
  "message": "Hello"
}
```

---

# 10. 第二步：啟動 Frontend

Frontend 在：

```
frontend/
```

可以使用 VS Code Extension：

```
Live Server
```

安裝 Live Server 後：

```
frontend/index.html
```

右鍵：

```
Open with Live Server
```

通常會開啟類似：

```
http://127.0.0.1:5500
```

---

# 11. 測試完整前後端

現在：

Backend：

```
http://127.0.0.1:8000
```

Frontend：

```
http://127.0.0.1:5500
```

在網頁輸入：

```
Hello
```

按：

```
Send
```

如果成功，應該可以看到 Backend 回覆。

現在第一次完成：

```
Frontend
   ↓
Backend
   ↓
Frontend
```

---

# 12. 最重要的 JavaScript：fetch()

打開：

```
frontend/script.js
```

今天最重要的是這一段：

```jsx
const response = await fetch(
    `${API_URL}/api/message`,
    {
        method: "POST",

        headers: {
            "Content-Type": "application/json"
        },

        body: JSON.stringify({
            message: message
        })
    }
);
```

可以把：

```
fetch()
```

想成：

```
Frontend 打電話給 Backend
```

---

# 13. Frontend 實際送什麼？

假設使用者輸入：

```
Hello
```

JavaScript：

```jsx
JSON.stringify({
    message: message
})
```

會產生：

```json
{
  "message": "Hello"
}
```

然後送到：

```
POST /api/message
```

---

# 14. Frontend 和 Backend 怎麼對起來？

Frontend：

```json
{
  "message": "Hello"
}
```

Backend：

```python
class MessageRequest(BaseModel):
    message: str
```

所以 Backend 可以使用：

```python
data.message
```

取得：

```
Hello
```

---

# 15. Backend 回傳之後呢？

Backend：

```python
return {
    "success": True,
    "reply": reply
}
```

會產生 JSON。

Frontend：

```jsx
const data = await response.json();
```

就可以取得：

```jsx
data.reply
```

接著更新畫面。

---

# 16. 今天最重要的一張圖

完整流程：

```
使用者輸入 Hello
        ↓
HTML input
        ↓
JavaScript
        ↓
sendMessage()
        ↓
fetch()
        ↓
POST /api/message
        ↓
FastAPI
        ↓
Python
        ↓
return JSON
        ↓
response.json()
        ↓
JavaScript
        ↓
更新 HTML
        ↓
使用者看到結果
```

如果這張圖看懂，今天最重要的觀念就學會了。

---

# 17. localhost 是什麼？

目前使用：

```
127.0.0.1
```

代表：

```
自己的電腦
```

例如：

```
http://127.0.0.1:8000
```

代表：

```
Backend 跑在自己的電腦
```

別人通常不能直接使用這個網址。

接下來要做：

```
localhost
↓
部署
↓
Public URL
```

---

# 18. 第三步：把 Backend 上傳 GitHub

建議 Backend 使用自己的 Repository。

例如：

```
simple-chat-backend
```

Repository 裡面：

```
main.py
requirements.txt
```

---

# 19. Git 基本流程

在 Backend 專案資料夾：

```bash
git init
```

加入檔案：

```bash
git add .
```

建立 Commit：

```bash
git commit -m "Initial backend"
```

到 GitHub 建立：

```
simple-chat-backend
```

再按照 GitHub 提供的指令連接 Repository。

通常類似：

```bash
git remote add origin https://github.com/YOUR_USERNAME/simple-chat-backend.git
```

然後：

```bash
git branch -M main
```

```bash
git push -u origin main
```

---

# 20. GitHub 不等於 Backend 已經在跑

現在只是：

```
Local Code
↓
GitHub
```

GitHub 幫你：

```
保存程式碼
```

但 Python 還沒有真正跑在 Internet。

所以接著需要：

```
Render
```

---

# 21. 第四步：Render 部署 FastAPI

登入 Render。

選：

```
New
↓
Web Service
```

連接：

```
GitHub
```

選擇：

```
simple-chat-backend
```

---

# 22. Render 設定

如果 Backend Repository 裡直接有：

```
main.py
requirements.txt
```

則：

Build Command：

```bash
pip install -r requirements.txt
```

Start Command：

```bash
uvicorn main:app --host 0.0.0.0 --port $PORT
```

方案選：

```
Free
```

建立 Web Service。

---

# 23. 如果你使用原本 ZIP 的單一 Repository

如果 GitHub Repository 結構是：

```
simple-fullstack-render-demo/
│
├── backend/
│   ├── main.py
│   └── requirements.txt
│
└── frontend/
```

Render 要多設定：

```
Root Directory:
backend
```

Build Command：

```bash
pip install -r requirements.txt
```

Start Command：

```bash
uvicorn main:app --host 0.0.0.0 --port $PORT
```

---

# 24. Render 成功後

成功部署會得到類似：

```
https://simple-chat-backend.onrender.com
```

每個人的網址都不同。

打開：

```
https://你的服務.onrender.com
```

應該看到：

```json
{
  "message": "Backend is running!"
}
```

---

# 25. 測試 Internet 上的 API

打開：

```
https://你的服務.onrender.com/docs
```

再測：

```
POST /api/message
```

這一次 Python 已經不是跑在自己電腦。

而是：

```
Internet 上的 Render Server
```

---

# 26. 第五步：Frontend 改成 Render URL

打開：

```
frontend/script.js
```

原本：

```jsx
const API_URL =
    "http://127.0.0.1:8000";
```

修改成 Render URL：

```jsx
const API_URL =
    "https://你的服務.onrender.com";
```

例如：

```jsx
const API_URL =
    "https://simple-chat-backend.onrender.com";
```

存檔。

---

# 27. 再測一次 Frontend

Frontend 仍然可以先用：

```
Live Server
```

但現在資料流變成：

```
自己的 Browser
       ↓
Frontend
       ↓
Internet
       ↓
Render
       ↓
FastAPI
```

這是一個非常重要的變化。

---

# 28. 第六步：部署 Frontend 到 GitHub Pages

建議建立另一個 Repository：

```
simple-chat-frontend
```

內容直接放：

```
index.html
style.css
script.js
```

結構：

```
simple-chat-frontend/
│
├── index.html
├── style.css
└── script.js
```

---

# 29. Push Frontend

在 Frontend 資料夾：

```bash
git init
```

```bash
git add .
```

```bash
git commit -m "Initial frontend"
```

建立 GitHub Repository：

```
simple-chat-frontend
```

連接後：

```bash
git branch -M main
```

```bash
git push -u origin main
```

---

# 30. 開啟 GitHub Pages

到 Frontend Repository：

```
Settings
↓
Pages
```

選：

```
Deploy from a branch
```

Branch：

```
main
```

Folder：

```
/root
```

儲存。

完成後會產生網址，類似：

```
https://USERNAME.github.io/simple-chat-frontend/
```

---

# 31. 最終架構

現在整個網站：

```
使用者
  ↓
GitHub Pages
  ↓
HTML / CSS / JavaScript
  ↓
fetch()
  ↓
Render
  ↓
FastAPI
  ↓
Python
  ↓
JSON
  ↓
Frontend
  ↓
使用者看到結果
```

這就是一個真正公開的：

```
Frontend + Backend Web Application
```

---

# 32. GitHub Pages 和 Render 各自負責什麼？

GitHub Pages：

```
Frontend
```

負責：

```
HTML
CSS
JavaScript
```

---

Render：

```
Backend
```

負責：

```
Python
FastAPI
API
```

---

# 33. 為什麼需要 CORS？

Frontend：

```
https://USERNAME.github.io
```

Backend：

```
https://xxx.onrender.com
```

兩個網址不同。

Browser 會有跨來源安全限制。

所以 Backend 使用：

```python
CORSMiddleware
```

教學版目前設定：

```python
allow_origins=["*"]
```

代表：

```
任何網站都可以呼叫 API
```

正式網站通常會限制允許的 Frontend 網址。

---

# 34. Render Free 的注意事項

免費 Render Web Service 很適合：

```
課堂 Demo
Side Project
Prototype
```

但如果一段時間沒有使用，服務可能會進入休眠。

因此第一次打開：

```
可能比較慢
```

不代表程式一定壞掉。

---

# 35. 常見 Debug：先檢查 Backend

如果網站不能用：

先直接打開：

```
https://你的服務.onrender.com
```

如果看不到：

```json
{
  "message": "Backend is running!"
}
```

先不要怪 Frontend。

代表 Backend 本身就有問題。

---

# 36. 再測 /docs

打開：

```
https://你的服務.onrender.com/docs
```

測：

```
POST /api/message
```

如果 `/docs` 正常：

```
Backend 大致正常
```

接著再檢查 Frontend。

---

# 37. 檢查 API_URL

Frontend 常見錯誤：

```jsx
const API_URL =
    "http://127.0.0.1:8000";
```

部署之後還忘了修改。

應該改成：

```jsx
const API_URL =
    "https://你的服務.onrender.com";
```

---

# 38. Render Build Failed 怎麼辦？

先看：

```
Render Logs
```

常見問題：

```
requirements.txt 不存在

Root Directory 錯誤

Python 套件沒有安裝

Start Command 寫錯

main.py 路徑錯誤
```

---

# 39. AI Coding Agent 練習 1：Clear Chat

現在可以開始搭配 Codex / Copilot。

Prompt：

```
I have a beginner-friendly frontend using:

- HTML
- CSS
- Vanilla JavaScript

It calls a FastAPI backend deployed on Render.

Please add a Clear Chat button.

Requirements:

1. Clicking it removes the displayed chat history.
2. Do not reload the webpage.
3. Do not use React.
4. Do not modify the backend.
5. Explain which HTML and JavaScript code you changed.
```

---

# 40. AI Coding Agent 練習 2：改善網站外觀

Prompt：

```
Improve the CSS of this beginner chat application.

Requirements:

- Keep the current HTML structure.
- Use only CSS.
- Make it look like a modern chat interface.
- Make it mobile friendly.
- Do not use Bootstrap.
- Do not use Tailwind.
- Explain the important CSS changes.
```

---

# 41. AI Coding Agent 練習 3：Backend 多回傳資料

Prompt：

```
Modify my FastAPI endpoint.

Current input:

{
  "message": "Hello world"
}

Please additionally return:

- character_count
- word_count

Keep the current reply field.

Then show the minimal JavaScript changes
needed to display these values.

Do not rewrite the whole project.
```

---

# 42. 使用 AI Coding Agent 的好方法

不要只說：

```
幫我做網站
```

比較好的方式：

```
目前有什麼
+
我要新增什麼功能
+
哪些東西不要改
+
請解釋修改內容
```

例如：

```
My project already has a working Send button.

Please add Enter-to-send.

Do not duplicate the existing fetch code.

Reuse the current sendMessage() function.

Explain the change.
```

---

# 43. AI 不應該只是幫你全部寫完

建議流程：

```
學生理解需求
↓
交給 AI 修改
↓
學生閱讀修改
↓
執行
↓
測試
↓
Debug
↓
繼續修改
```

而不是：

```
一句 Prompt
↓
AI 全寫
↓
完全不知道程式在做什麼
```

---

# 44. 今天至少要會回答

完成後，應該能回答：

- Frontend 是什麼？
- Backend 是什麼？
- API 是什麼？
- `fetch()` 在做什麼？
- POST 在做什麼？
- JSON 是什麼？
- localhost 是什麼？
- Render 在做什麼？
- GitHub Pages 在做什麼？
- 為什麼 Frontend 和 Backend 可以放不同地方？
- 為什麼 API Key 不應該直接放在 Frontend？

---

# 45. 最終 Challenge

請使用 Codex / Copilot 幫網站增加至少一個功能：

可以選：

- Clear Chat
- Dark Mode
- Enter 傳送
- Loading Animation
- Backend 字數統計
- Message Timestamp
- 更漂亮的 Chat Bubble
- Error Message
- Mobile Layout

規則：

```
AI 可以幫你寫

但你必須能解釋：

1. 改了哪個檔案
2. 為什麼要改
3. 資料怎麼流動
```

---

# 46. 最重要的資料流

最後再看一次：

```
使用者
  ↓
HTML
  ↓
JavaScript Event
  ↓
fetch()
  ↓
HTTP Request
  ↓
FastAPI
  ↓
Python
  ↓
JSON Response
  ↓
JavaScript
  ↓
DOM Update
  ↓
使用者看到結果
```

今天真正要記住的是這條線。

---

# 47. 下一堂可以做什麼？

## 路線 A：真的接 AI

```
Frontend
↓
FastAPI
↓
OpenRouter / LLM API
↓
AI 回答
```

---

## 路線 B：加入 Database

```
Frontend
↓
FastAPI
↓
PostgreSQL
↓
永久保存留言
```

---

## 路線 C：登入系統

```
Frontend
↓
Authentication
↓
不同使用者
↓
自己的資料
```

---

# 最後一句

```
GitHub Pages 讓 Frontend 公開。

Render 讓 Python Backend 公開。

fetch() 把兩邊接起來。
```