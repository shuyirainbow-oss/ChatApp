# 今日部署成功流程筆記

日期：2026-08-15

## 今日完成的成果

- 完成本機前後端測試。
- 將完整專案推送到 GitHub。
- 成功將 FastAPI 後端部署到 Render。
- 將前端 API 位址改為 Render 後端網址，並推送到 GitHub。

---

## 1. 本機專案結構

```text
simple-fullstack-render-demo/
├── backend/
│   ├── main.py
│   └── requirements.txt
├── frontend/
│   ├── index.html
│   ├── style.css
│   └── script.js
├── README.md
└── 前後端部署.md
```

本機測試時：

```bash
cd backend
pip install -r requirements.txt
uvicorn main:app --reload
```

後端本機網址：

```text
http://127.0.0.1:8000
```

前端可用 VS Code Live Server，或執行：

```bash
cd frontend
python -m http.server 5500
```

前端本機網址：

```text
http://127.0.0.1:5500
```

---

## 2. 推送完整專案到 GitHub

GitHub repository：

```text
https://github.com/shuyirainbow-oss/ChatApp.git
```

設定 Git 提交作者：

```bash
git config --global user.name "Shuyirainbow"
git config --global user.email "shuyirainbow@gmail.com"
```

標準推送流程：

```bash
git status
git add .
git commit -m "描述這次修改"
git push
```

專案使用 `.gitignore` 排除以下本機檔案，不會推送到 GitHub：

```text
.venv/
python.venv/
__pycache__/
.env
```

---

## 3. Render 後端部署（已成功）

在 Render：

```text
New → Web Service → 選 ChatApp repository
```

後端設定：

| 欄位 | 設定值 |
| --- | --- |
| Name | `ChatApp`（或 `chatapp-backend`） |
| Branch | `main` |
| Region | `Singapore` |
| Root Directory | `backend` |
| Language | `Python 3` |
| Build Command | `pip install -r requirements.txt` |
| Start Command | `uvicorn main:app --host 0.0.0.0 --port $PORT` |
| Health Check Path | `/` |

部署成功後的後端網址：

```text
https://chatapp-vcss.onrender.com
```

測試網址：

```text
https://chatapp-vcss.onrender.com
https://chatapp-vcss.onrender.com/docs
```

在 `/docs` 可測試：

```text
POST /api/message
```

測試內容：

```json
{
  "message": "Hello"
}
```

若回傳 JSON，即代表後端 API 正常。

> Render Free instance 閒置後可能休眠，第一次請求可能需要等待約 50 秒以上。

---

## 4. 前端連接 Render 後端（已完成）

檔案：`frontend/script.js`

設定為：

```javascript
const API_URL = "https://chatapp-vcss.onrender.com";
```

注意：JavaScript 必須填入純網址，不能填 Markdown 格式：

```javascript
// 錯誤示範：不要使用
const API_URL = "[https://chatapp-vcss.onrender.com](https://chatapp-vcss.onrender.com)";
```

修改後已推送到 GitHub，提交：

```text
33cb4bb Connect frontend to Render backend
```

---

## 5. Render 前端部署（下一步，尚未完成）

前端是 HTML、CSS、JavaScript 靜態檔案，必須選：

```text
New → Static Site
```

**不要選 Web Service**。Web Service 會要求 Python/Node 的 Start Command，不適合本專案前端。

Static Site 設定：

| 欄位 | 設定值 |
| --- | --- |
| Name | `chatapp-frontend` |
| Branch | `main` |
| Region | `Singapore` |
| Root Directory | `frontend` |
| Build Command | `echo "No build step"` |
| Publish Directory | `.` |

按下 **Create Static Site**，部署成功後 Render 會提供一個前端網址，例如：

```text
https://chatapp-frontend.onrender.com
```

開啟該網址即可使用網頁；送出訊息時，前端會呼叫已部署的 FastAPI 後端。

---

## 6. 資料庫部署（尚未開始）

目前程式沒有資料庫模型、連線設定或資料表，因此尚不能直接部署資料庫。

下一階段建議：

```text
選擇 PostgreSQL
→ 在 Render 建立 PostgreSQL Database
→ 將連線網址放入 Render Environment Variables
→ 修改 FastAPI 使用資料庫
→ git commit / git push
→ Render 自動重新部署後端
```
